import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "./App.tsx";

createRoot(document.getElementById("_paperclips_1mm2_3")!).render(
  <StrictMode>
    <App />
  </StrictMode>
);
